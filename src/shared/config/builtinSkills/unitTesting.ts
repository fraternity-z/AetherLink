import type { Skill } from '../../types/Skill';

export const unitTestingSkill: Skill = {
  id: 'builtin-unit-testing',
  name: '单元测试',
  description: '为代码生成全面的单元测试，覆盖正常路径、边界条件和异常场景',
  emoji: '🧪',
  tags: ['编程', '测试', '质量'],
  content: `# 单元测试技能

## 测试编写原则

1. **AAA 模式** — Arrange（准备）、Act（执行）、Assert（断言）
2. **单一职责** — 每个测试只验证一个行为
3. **独立性** — 测试之间互不依赖，可独立运行
4. **可读性** — 测试名称描述被测行为和预期结果

## 覆盖维度

### 正常路径（Happy Path）
- 典型输入的正常输出
- 各种合法参数组合

### 边界条件（Edge Cases）
- 空值/null/undefined
- 空字符串、空数组、空对象
- 最大值/最小值
- 单元素集合

### 异常场景（Error Cases）
- 非法参数类型
- 超出范围的值
- 网络错误/超时
- 并发竞态

## 测试命名规范

\`\`\`
describe('被测模块/函数', () => {
  it('should [预期行为] when [条件]', () => {
    // ...
  });
});
\`\`\`

## 输出格式

根据代码语言自动选择测试框架：
- TypeScript/JavaScript → Jest / Vitest
- Python → pytest
- Java → JUnit 5
- Go → testing 标准库`,
  triggerPhrases: ['写测试', '单元测试', 'unit test', 'write test', '生成测试'],
  source: 'builtin',
  version: '1.0.0',
  author: 'AetherLink',
  enabled: true,
  createdAt: '2026-02-27T00:00:00.000Z',
  updatedAt: '2026-02-27T00:00:00.000Z',
};
